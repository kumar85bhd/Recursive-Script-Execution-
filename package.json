{
  "name": "rtl-workflow",
  "private": true,
  "version": "1.0.0",
  "description": "RTL Workflow Automation",
  "scripts": {
    "dev": "node -e \"require('http').createServer((req, res) => { res.writeHead(200, {'Content-Type': 'text/plain'}); res.end('RTL Workflow Automation Environment is Ready.'); }).listen(3000); console.log('Dummy server running on port 3000');\"",
    "start": "npm run dev"
  }
}
